const styles = {
  container: {
    background: "#f2f2f2", // Light silver background color
    border: "1px solid #c0c0c0", // Light silver border color
    borderRadius: "4px", // Rounded border corners
    padding: "10px", // Padding around the content
    // Additional properties for visibility
    position: "relative", // Ensures the div is not affected by other elements' styles
    zIndex: 9999, // Puts the div on top of other elements
    boxShadow: "0px 0px 5px rgba(0, 0, 0, 0.1)", // Adds a subtle shadow to the div
    // Add any other necessary styling properties
  },
};

export default styles;
